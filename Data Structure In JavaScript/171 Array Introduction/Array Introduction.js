const strings=['a','b','c','d'];
//4*4=16 bytes of storage in a 32-bit system

strings[2];

//push
//strings.push('e');

//pop
strings.pop();

strings.unshift('x');
strings.splice(2,0,'alien');

console.log(strings);